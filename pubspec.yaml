name: chuspita
description: "Gestor financiero personal."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1
  flutter: 3.47.1

dependencies:
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  excel: ^4.0.6
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  intl: ^0.20.3
  path_provider: ^2.1.6
  share_plus: ^13.3.0
  shared_preferences: ^2.5.5
  sqlite3: ^3.5.2
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.5
  build_runner: ^2.16.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/branding/chuspita_logo_transparent.png
